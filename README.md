# Assignment 3 – Part 2

This repository contains the solutions for SYSC 4001 Assignment 3 Part 2:

- `ta_marking_a_101308579_101299663.cpp` – Part 2.a (shared memory, no semaphores)
- `ta_marking_b_101308579_101299663.cpp` – Part 2.b (shared memory + semaphores)

Both programs use the same input files:

- `rubric.txt` in the repo root
- `exams/` directory containing `exam_0001.txt` … `exam_0019.txt` and `exam_9999.txt`

---

## Compiling

### Option 1 – Using `build.sh` (recommended)

From the repository root:

```bash
chmod +x build.sh    # only needed once
./build.sh
